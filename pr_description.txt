🎯 **What:** The code health issue addressed is the usage of generic \`any\` and \`any[]\` types in \`useState\` hooks within \`AdminProjectView.tsx\`. We added explicit DTO typings (\`ProjectDTO\`, \`AssetDTO\`, \`CommitDTO\`, \`DeployDTO\`) and typed the respective state variables.
💡 **Why:** Explicit typing ensures compiler checks which helps prevent runtime crashes due to undefined properties, improves maintainability, and provides better IDE intellisense over using \`any\`.
✅ **Verification:** Verified by running \`npx tsc --noEmit\` to ensure type safety and \`pnpm test\` to ensure no functional regressions occurred within the test suite.
✨ **Result:** The component states are now strictly typed improving overall code health without breaking behavior.
