Title: "🧪 [add tests for Login page]"

🎯 What:
Added a missing test file `src/pages/Login.test.tsx` for the `Login.tsx` component to cover form rendering, input changing, error handling, API submission, and authentication redirections.

📊 Coverage:
The following scenarios are now tested:
- Form elements are rendered correctly (inputs and buttons).
- Toggling the password visibility correctly switches the input type.
- Form submissions with empty inputs trigger validation error messages and prevent API calls.
- Successful API call correctly stores tokens in localStorage, navigates to the dashboard, and shows a success toast.
- Failed API call shows a validation error message in the UI and an error toast.
- Pre-existing authentication redirects the user to `/project-hub` on component load.

✨ Result:
Improved test coverage, creating a reliable safety net that covers user authentication workflows before accessing the application dashboard, allowing for more confident future refactorings on the Login page.
